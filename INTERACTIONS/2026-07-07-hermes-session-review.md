---
date: 2026-07-07
type: session-review
agent: CHatz (Hermes Agent)
source: Telegram DM
tags: [hermes, infrastructure, providers, oblivion, c2, opencode, 9router]
---

# Hermes Session Review — 2026-07-07

## Overview

Major infrastructure session: established multiple AI provider fallbacks, configured local LLM, audited C2 Redis guards, secured API keys, and setup 9Router as an AI routing layer.

---

## 1. Provider Setup

### Local LLM (Primary)
- **Endpoint:** `http://100.102.204.115:9000/v1` (laptop llama.cpp via Tailscale)
- **Model:** `Qwen3.5-9B-Uncensored-HauhauCS-Aggressive-Q4_K_M.gguf`
- **Config:** `model.provider: custom:local`, `model.base_url: http://100.102.204.115:9000/v1`
- **Status:** ✅ Working — tested with direct chat completion
- **Note:** Requires `/reset` to take effect in Hermes session

### API Keys Added to `~/.hermes/.env`

| Provider | Env Var | Status |
|----------|---------|--------|
| Gemini | `GOOGLE_API_KEY` / `GEMINI_API_KEY` | ✅ Verified |
| OpenRouter (2nd key) | `OPENROUTER_API_KEY2` | ✅ Stored |
| Groq | `GROQ_API_KEY` | ✅ Stored |
| Cerebras | `CEREBRAS_API_KEY` | ✅ Stored |
| Cloudflare Workers AI | `CLOUDFLARE_API_KEY` | ✅ Stored |
| LLM7.io | `LLM7_API_KEY` | ✅ Stored |
| Ollama Cloud | `OLLAMA_CLOUD_API_KEY` | ✅ Stored |
| Chutes | `CHUTES_API_KEY` | ✅ Stored |
| Cortex | `CORTEX_API_KEY` | ✅ Stored |

### OpenCode Go API Key
- **Key:** Set as `OPENCODE_GO_API_KEY` in `.env`
- **Endpoint:** `https://opencode.ai/zen/go/v1`
- **Status:** ✅ Verified via curl (direct API call works, `opencode run` hangs within nested Hermes session)

---

## 2. 9Router Integration

- **Repo:** `https://github.com/decolua/9router` (⭐20.6K stars)
- **Description:** FREE AI Router & Token Saver — connects 40+ providers, 100+ models, auto-fallback, RTK token compression
- **Setup:** `hermes skills tap add https://github.com/decolua/9router`
- **Endpoint:** OpenAI-compatible at `http://localhost:20128/v1` when running
- **Models:** Uses `kr/` (Kiro), `cc/` (Claude Code), `glm/`, `minimax/`, `if/` (iFlow) prefixes
- **Key Features:** RTK Token Saver (20-40% savings), Smart 3-tier fallback, Caveman/Ponytail modes
- **Status:** ⏳ Tapped as skill source — needs local deployment to use

---

## 3. Oblivion C2 — Redis Guards & UI Fixes

### Backend (`brain.py`)
- **Problem:** `self.c2.redis.*` calls would crash when `redis_ready=False`
- **Fix:** Guarded ALL Redis operations with `if redis_ready` checks across:
  - `log_state()` — Redis store
  - `log_cmd()` — Redis store
  - `talk()` — Redis store
  - `get_stats()` — cluster stats fetch
  - `_get_model()` — Redis cache
  - `map()` — Redis-side store
  - Endpoint `/redteam/attack-tree` — Redis fetch
  - Endpoint `/operant/prompt` — Redis store
  - Endpoint `/sinister-chaos/command` — Redis store
  - `_initialize_agents()` — Redis agent store
  - `_initialize_firewall()` — Redis store
- **Pattern:** Each guard returns empty/fallback values when Redis is unavailable

### Frontend (ControlPanel.tsx)
- **Sim Timer UI:** Red STOP button + runtime elapsed counter
- **Animation:** Static typing dots replaced with Framer Motion animated spans (bounce, scale, opacity)
- **Build:** `npm run build` — webpack/vite produces `/dist/assets/index-*.js`

### Deployment
- **Pushed to Railway:** `https://sinisterchaos.xyz`
- **Git:** `git add -A && git commit -m "Guard remaining brain.py Redis calls + sim timer UI + anim dots" && git push`
- **Branch:** Not specified — likely `master` or `main`

---

## 4. Current Hermes Config State

```yaml
model:
  default: Qwen3.5-9B-Uncensored-HauhauCS-Aggressive-Q4_K_M.gguf
  provider: custom:local
  base_url: http://100.102.204.115:9000/v1
  api_key: ''
  context_length: 262144
```

**Active Toolsets:** web, terminal, file, vision, delegation, cronjob, memory, session_search, skills

---

## 5. Infrastructure Layout

| Component | Location | Status |
|-----------|----------|--------|
| Laptop LLM (llama.cpp) | `parrot-1:9000` (Tailscale) | ✅ |
| Docker Stack | `100.102.204.115` (Postgres/Redis/Qdrant/n8n) | ✅ |
| C2 Dashboard | `https://sinisterchaos.xyz` (Railway) | ✅ |
| Obsidian Vault | `/root/obsidian-vault/` (server clone) | ✅ |
| Business Site | `indicationsmedia.vercel.app` (Vercel) | ✅ |
| RE-Ai Bot | `@REAi_bot` (Telegram) | ✅ |

---

## 6. Pending / To Do

- [ ] `/reset` Hermes session to activate local LLM provider
- [ ] Deploy 9Router locally (`npm install -g 9router`) for free AI routing
- [ ] Verify OpenCode Go API key works in a fresh Hermes session
- [ ] Add new OpenRouter key to credential pool via `hermes auth add openrouter`
- [ ] Write the session review user originally requested before they pivoted to providers

---

*Generated by CHatz — session end 2026-07-07*